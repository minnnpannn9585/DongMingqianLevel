using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class diary : MonoBehaviour
{
    public PlayerGrab playerGrab;
    public Text diaryBoxText;
    public GameObject diaryBox;
    public string diaryText;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*if(playerGrab.isPlayerInDiary)
        {
            diaryBoxText.text = diaryText;
            diaryBox.SetActive(true);
        }
        if(Input.GetKeyUp(KeyCode.W) ||  Input.GetKeyUp(KeyCode.S) || Input.GetKeyUp(KeyCode.D) || Input.GetKeyUp(KeyCode.A))
        {
            diaryBox.SetActive(false);
            playerGrab.isPlayerInDiary = false;
        }*/
    }

  



}
